# 🚀 Quick Start - Run in 3 Steps!

## For Complete Beginners

### Step 1: Install Node.js
👉 Go to https://nodejs.org/ and download the **LTS version**
- Click the big green button
- Run the installer
- Click "Next" until it's done
- Restart your computer

### Step 2: Open Terminal/Command Prompt

**Windows:**
1. Press `Windows key + R`
2. Type `cmd` and press Enter

**Mac:**
1. Press `Command + Space`
2. Type `terminal` and press Enter

**Linux:**
1. Press `Ctrl + Alt + T`

### Step 3: Run These Commands

Copy and paste these commands one by one:

```bash
# Navigate to the project folder (change the path to where you extracted the files)
cd path/to/strokeaware-education-platform

# Install dependencies (this takes 2-5 minutes)
npm install

# Start the application
npm run dev
```

### Step 4: Open Your Browser

Go to: **http://localhost:5173**

---

## 🎉 That's It!

You should now see the StrokeAware platform running!

---

## ⚠️ Having Issues?

Read the **SETUP_GUIDE.md** file for detailed troubleshooting.

Common fixes:
- Make sure Node.js is installed: `node --version`
- Make sure you're in the right folder: `ls` (Mac/Linux) or `dir` (Windows)
- Delete `node_modules` folder and run `npm install` again
- Try a different browser

---

## 🛑 To Stop the Server

Press `Ctrl + C` in the terminal

---

## 📝 Quick Reference

| Command | What it does |
|---------|-------------|
| `npm install` | Installs all dependencies (run once) |
| `npm run dev` | Starts development server |
| `npm run build` | Creates production build |
| `npm run preview` | Preview production build |

---

**Need more help?** Check **SETUP_GUIDE.md** for complete instructions!
